# تشغيل بوت الحضور والانصراف

## الموجود في البوت

- لوحة أزرار: **تسجيل دخول**، **تسجيل خروج**، **عرض ساعاتي**.
- زر **Language / اللغة** ثابت للجميع لاختيار لغة الرسائل.
- الموظف يستخدم الأزرار فقط؛ لا تظهر له أوامر تسجيل الدخول والخروج أو عرض الساعات.
- عند الضغط على زر الدخول أو الخروج، تظهر نافذة يرفع فيها العضو ملف صورة أو يضع رابط الصورة؛ بدون أحدهما لا يتم الحفظ.
- اختيار اللغة محفوظ لكل عضو داخل السيرفر حتى يختار لغة أخرى.
- روم مركزي يظهر فيه اسم العضو، نوع العملية، وقتها، الصورة، ورقم السجل.
- منع الدخول مرتين أو الخروج بدون دخول.
- زر **عرض ساعاتي** يعرض ساعات العضو خلال آخر 7 أيام.
- زر **الجرد الأسبوعي** متاح لرتبة الجرد والإدارة.
- تقرير تلقائي كل يوم أحد الساعة 23:55 بتوقيت القاهرة في روم السجل.
- سجل أحداث ثابت في قاعدة البيانات: التصحيح أو الإضافة مستقبلًا يمكن أن يظهر كسجل جديد بدل حذف الأثر القديم.

## الإعداد داخل السيرفر

1. من Discord Developer Portal افتح تطبيق البوت، ثم **Bot → Reset Token** إذا كان التوكن القديم ظهر في رسالة أو مكان غير آمن.
2. تأكد أن البوت مضاف للسيرفر بصلاحيات:
   - `View Channels`
   - `Send Messages`
   - `Embed Links`
   - `Attach Files`
   - `Read Message History`
   - `Use Application Commands`
3. شغّل البوت من المشروع. أوامر البوت تظهر بعد دخوله للسيرفر.
4. من السيرفر نفّذ:
   - `/setup-channel` واختر روم تسجيل الحضور والانصراف والصور.
   - `/setup-roles` واختر رتبة الجرد، ويمكن اختيار رتبة إدارة البوت.
   - `/attendance-panel` في الروم الذي تريد وضع أزرار الحضور فيه.
   - `/who-is-in` لمعرفة الأعضاء الذين لديهم تسجيل دخول مفتوح حاليًا؛ الأمر متاح لرتبة الجرد والإدارة فقط.
   - `/add-hour` لاختيار عضو وإضافة عدد ساعات له، ويمكن إضافة سبب اختياري.
   - `/remove-hour` لاختيار عضو وخصم عدد ساعات منه، ويمكن إضافة سبب اختياري.

## طريقة الاستخدام

- العضو يضغط **تسجيل دخول**، ثم يرفع صورة أو يضع رابط الصورة في النافذة. بدون صورة أو رابط لا يتم تسجيل الدخول.
- عند نهاية العمل يكرر نفس الخطوات مع **تسجيل خروج**.
- العضو يضغط **عرض ساعاتي** لعرض سجله.
- مسؤول الجرد يضغط **الجرد الأسبوعي** من لوحة الأزرار.
- العضو يضغط **Language / اللغة** ثم يختار **العربية** أو **English**؛ بعد ذلك تظهر له رسائل البوت وتقاريره باللغة المختارة.
- أوامر إضافة وخصم الساعات متاحة لرتبة الجرد المختارة في `/setup-roles` والإدارة، وتظهر في اللوقات مع اسم العضو، مقدار التعديل، المنفّذ، والسبب.

## ملاحظات الأمان

- لا تشارك `DISCORD_BOT_TOKEN` في الشات أو داخل الملفات.
- الصورة وحدها ليست دليلًا كافيًا لمنع التلاعب؛ البوت يحتفظ أيضًا بوقت العملية، رقم رسالة Discord، رابط المرفق، وهوية منفّذ العملية.
- لا توجد أوامر لتعديل أو حذف السجلات الأصلية في النسخة الحالية.

## تشغيل البوت على Railway

يوجد ملف `railway.json` جاهز لتشغيل API والبوت كخدمة مستمرة مع إعادة التشغيل تلقائيًا عند التعطل.

بعد ربط المستودع في Railway:

1. أضف خدمة PostgreSQL في نفس المشروع.
2. أضف المتغير `DATABASE_URL` من خدمة قاعدة البيانات.
3. افتح خدمة البوت في Railway ثم **Variables → New Variable**.
4. أضف اسم المتغير بالضبط: `DISCORD_BOT_TOKEN`.
5. ضع قيمة التوكن في خانة القيمة، ثم احفظ وأعد تشغيل الخدمة.
6. شغّل مرة واحدة من بيئة Railway:
   `pnpm --filter @workspace/db run push`
7. أعد النشر وتأكد من ظهور سجل `Discord attendance bot is ready`.

التوكن يتم قراءته من `process.env.DISCORD_BOT_TOKEN` وقت التشغيل؛ لا تضعه في
`attendance-bot.ts` أو `railway.json` أو أي ملف داخل المستودع.

Railway يحدد `PORT` تلقائيًا، والسيرفر يستخدمه مباشرة.